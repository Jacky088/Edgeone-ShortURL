# EdgeOne 短链接系统 - 环境变量配置
# 复制此文件为 .env 并填写实际值（开发环境）
# 生产环境请在 EdgeOne Pages 控制台设置

# ============================================
# 认证配置（必需）
# ============================================

# 管理员密码
# 要求：至少 16 字符，包含大小写字母、数字、特殊字符
# 示例：9K#mP$2vL@8xQ!wE
ADMIN_PASSWORD=your_secure_password_here

# 管理路径（用于访问后台）
# 要求：随机字符串，避免使用 admin 等常见路径
# 示例：admin_xyz_789_secret
ADMIN_PATH=your_secret_admin_path_here

# ============================================
# 会话配置（必需）
# ============================================

# 会话签名密钥
# 要求：至少 32 字符，使用加密随机生成器
# 生成方法（PowerShell）：
# $bytes = New-Object byte[] 32
# [Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
# [Convert]::ToBase64String($bytes)
SESSION_SECRET=your_session_secret_key_must_be_at_least_32_chars

# CSRF 令牌密钥
# 要求：至少 32 字符，使用加密随机生成器
CSRF_SECRET=your_csrf_secret_key_must_be_at_least_32_chars

# ============================================
# 数据库配置（EdgeOne Pages 自动绑定）
# ============================================

# D1 数据库绑定变量名：DB
# 在 EdgeOne Pages 控制台设置，无需在此配置

# ============================================
# 安全提示
# ============================================

# 1. 不要将此文件提交到 Git 仓库（已在 .gitignore 中）
# 2. 生产环境请使用 EdgeOne Pages 控制台的加密环境变量
# 3. 定期轮换所有密钥（建议每 3 个月）
# 4. 每个环境使用不同的密钥
# 5. 详见 DEPLOYMENT.md 了解完整部署指南

# ============================================
# 可选配置（高级）
# ============================================

# 以下配置在代码中硬编码，如需修改请编辑 functions/lib/security.js

# 速率限制（默认值）：
# - 登录：5 次/15 分钟
# - 创建短链接：10 次/分钟
# - 删除短链接：20 次/分钟
# - 获取链接列表：20 次/分钟

# 会话过期时间：
# - 绝对过期：24 小时
# - 空闲超时：30 分钟

# 审计日志保留期：90 天

# PBKDF2 迭代次数：100,000 次
